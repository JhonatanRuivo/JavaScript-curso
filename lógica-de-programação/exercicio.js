// -------------------------- PROMPT DO NAVEGADOR -------------------------------------------
var name = prompt('Digite seu nome') // o prompt pode ser muito útil para pegar informações que o usuário digitar no input do prompt
var age = prompt('Digite sua idade')
var city = prompt('Digite sua cidade')

console.log(`${name} de ${age} anos morador de ${city}`) // aqui veremos o valor da variável name que o usuário preencheu no input do prompt
